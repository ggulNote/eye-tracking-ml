# 프로젝트 공통 지침

## 기본 원칙

- 머신러닝 실험, 모델 관리 및 버전 관리는 MLflow를 중심으로 구성한다.
- 값, 경로, 모델 버전, 입출력 규격을 코드에 하드코딩하지 않는다.
- 설정(config) 기반으로 각 기능을 모듈화하여 구성한다.

## 입출력 규격

- 모델과 전처리 모듈마다 input/output의 타입, 크기(shape) 및 형식을 명시한다.
- 동일한 인터페이스에서는 input/output 크기와 형식이 항상 일정하게 유지되도록 한다.
- 규격이 맞지 않으면 실행 초기에 명확한 오류를 발생시키도록 검증한다.

## 전처리 및 모델 버전 관리

- 전처리 파이프라인은 모델 코드와 분리하고 재사용 가능한 모듈로 구축한다.
- 모델 버전이 변경되어도 최소한의 설정 변경만으로 새로운 모델을 연결할 수 있어야 한다.
- 모델 버전과 해당 전처리 파이프라인의 조합을 MLflow에서 추적할 수 있도록 한다.
- 주요 파라미터, 메트릭, 모델 및 전처리 산출물을 MLflow에 기록한다.

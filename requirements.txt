# Runtime and training/evaluation dependencies for CPython 3.12.x.
# These are direct dependencies. 배포 시 platform-specific lock file 또는
# container digest를 함께 고정합니다.

torch==2.13.0
torchvision==0.28.0
torchmetrics==1.9.0
# Official WebEyeTrack weights are a backend-neutral Keras v3 archive.
# The pipeline loads them with KERAS_BACKEND=torch and fine-tunes them via PyTorch.
keras==3.15.1

numpy==2.5.1
# MLflow 3.14 requires pandas<3.
pandas==2.3.3
scipy==1.18.0
scikit-learn==1.9.0

Pillow==12.3.0
opencv-contrib-python==4.13.0.92
mediapipe==0.10.35

hydra-core==1.3.4
omegaconf==2.3.0
pydantic==2.13.4
PyYAML==6.0.3

mlflow==3.14.0
# Required when mlflow.log_system_metrics=true (the default config).
psutil==7.0.0
matplotlib==3.11.1
tqdm==4.67.1

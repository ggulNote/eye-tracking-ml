# Runtime and planned-training dependencies for CPython 3.12.x.
# These are direct dependencies. After the first end-to-end implementation is
# verified, export a platform-specific lock file or container digest as well.

torch==2.13.0
torchvision==0.28.0
torchmetrics==1.9.0

numpy==2.5.1
# MLflow 3.14 requires pandas<3.
pandas==2.3.3
scipy==1.18.0
scikit-learn==1.9.0

Pillow==12.3.0
opencv-contrib-python==4.13.0.92
mediapipe==0.10.35

hydra-core==1.3.4
omegaconf==2.3.0
pydantic==2.13.4
PyYAML==6.0.3

mlflow==3.14.0
# Required when mlflow.log_system_metrics=true (the default config).
psutil==7.0.0
matplotlib==3.11.1
tqdm==4.67.1
